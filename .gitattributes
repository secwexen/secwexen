# ─────────────────────────────────────────────
# Git attributes – X99874 profile repository
# Configuration for line-ending consistency, readable diffs, and accurate GitHub language statistics
# ─────────────────────────────────────────────

# Markdown files (docs, profile, policies)
*.md               text linguist-language=Markdown

# YAML configuration files (profile, security, network, etc.)
*.yaml             text linguist-language=YAML
*.yml              text linguist-language=YAML

# Shell scripts (setup, aliases, automation)
*.sh               text eol=lf linguist-language=Shell

# Python scripts (tests, automation)
*.py               text eol=lf linguist-language=Python

# JSON files (metadata, config)
*.json             text linguist-language=JSON

# Git configuration and dotfiles
.zshrc             text
.vimrc             text
.aliases.sh        text
.gitconfig         text

# Ignore diff for binary assets
*.png              binary
*.jpg              binary
*.jpeg             binary
*.gif              binary
*.ico              binary

# SVGs are text-based and diffable
*.svg              text

# Windows batch files (if any)
*.bat              text eol=crlf

# Treat config and dotfiles as documentation (not code)
config/**          linguist-documentation
dotfiles/**        linguist-documentation

# Normalize line endings for all text files
*                  text=auto
