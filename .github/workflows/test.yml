name: Run Echo on Push

on:
  push:
    branches:
      - main

jobs:
  echo:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Echo test
      run: echo "GitHub Actions is working!"
